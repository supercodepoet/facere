<wa-dialog id="delete-dialog-<%= todo_list.id %>" class="delete-dialog" label="Delete confirmation">
  <div class="delete-modal-content">
    <div class="delete-modal-icon">
      <wa-icon name="trash" variant="thin"></wa-icon>
    </div>

    <h2>Delete this list?</h2>
    <p>This will permanently delete all tasks in this list. You won't be able to undo this action.</p>

    <div class="delete-modal-actions">
      <button class="delete-cancel-btn" data-action="click->delete-confirmation#close">
        Cancel
      </button>
      <%= button_to todo_list_path(todo_list), method: :delete, class: "delete-confirm-btn" do %>
        <wa-icon name="trash" variant="thin"></wa-icon>
        Delete
      <% end %>
    </div>
  </div>
</wa-dialog>
