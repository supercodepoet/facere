# Definition

Use the `TODO Detail Item` in `todo-list-item-screens.pen` file as the source of truth and the visual requirement for this screen. You need to use the colors and flows of this screen. If you have a better way, then ask. Make sure to use our existing technology stack but if a dependancy makes sense to ease development, you can add that. Make sure to keep in mind the Fizzy project (https://github.com/basecamp/fizzy) by 37 Signals as a reference on how Ruby on Rails code should be written, architected, and constructed.

# Requirements
- We now want to use the new Lexxy editor as the tool for editing the content of the Notes section
- One change from the UI/UX of `TODO Detail item`, we want to have the Notes section always editable without needing to click a button. It will also auto-save on change.
- There are other screens in `todo-list-item-screens.pen` for reference of interactions and flow.