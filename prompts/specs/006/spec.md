# Definition

It is time to polish the things not working on both our TODO list screen and our TODO item screen. You need to refer to the `todo-list-item-screens.pen` file for the visual reference, visual source of truth, and the source of truth for the business process flow. The screens to use for the start of the polish are:

- `TODO List Detail`
- `TODO Item Detail`

# Requirements

`TODO List Detail`:
- Review and add the pills on the item display
- Let's add the context menu for sections and items

`TODO Item Detail`:
- For Notes, after you are done Editing make the button Save instead of Done and then save the content and make sure the display is updated with the changes
- For Notes, let's make sure we update the button UI to make the visual reference, so far it does not
- For Assigned to, let's now allow the selecting from the owner or shared users
- Let's make sure we can set a due date
- For Notify on Complete, let's now allow selecting from the owner or shared users
- For Attachments, let's make sure we can store files and show them in the listing